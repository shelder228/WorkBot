# Инструкция по развертыванию бота

Это руководство поможет вам развернуть бота на различных платформах.

## 🆓 Быстрый старт (бесплатные варианты)

**Рекомендуется для начинающих:**
1. **Railway** - самый простой, $5 кредитов/месяц (обычно хватает) ⭐
2. **Render** - простой, но может засыпать после 15 мин неактивности
3. **Fly.io** - не засыпает, 3 бесплатных VM
4. **PythonAnywhere** - полностью бесплатно, но нужно обновлять задачу раз в день

**Выберите один из вариантов ниже и следуйте инструкциям.**

## 🆓 Бесплатные варианты (рекомендуется)

### 1. 🚀 Railway (Рекомендуется - самый простой) ⭐ БЕСПЛАТНО

Railway - это платформа, которая автоматически развертывает ваш код.

#### Шаги:

1. **Создайте аккаунт на Railway:**
   - Перейдите на https://railway.app
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - В настройках проекта найдите "Variables"
   - Добавьте:
     - `BOT_TOKEN` - токен вашего бота от @BotFather
     - `ADMIN_ID` - ваш Telegram ID (можно узнать у @userinfobot)

4. **Запустите деплой:**
   - Railway автоматически определит Python проект
   - Бот запустится автоматически

**Плюсы:** 
- ✅ **Бесплатный тариф** ($5 кредитов каждый месяц)
- ✅ Автоматический деплой из GitHub
- ✅ Простота использования
- ✅ Автоматические обновления при push

**Ограничения бесплатного тарифа:**
- $5 кредитов в месяц (обычно хватает для одного бота)
- После использования кредитов сервис останавливается

---

### 2. ☁️ Render ⭐ БЕСПЛАТНО

Render - еще одна простая платформа для деплоя.

#### Шаги:

1. **Создайте аккаунт на Render:**
   - Перейдите на https://render.com
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Или используйте файл `render.yaml` (он уже настроен)

3. **Настройте сервис (если не используете render.yaml):**
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
   - **Environment:** Python 3

4. **Добавьте переменные окружения:**
   - В разделе "Environment Variables" добавьте:
     - `BOT_TOKEN` - токен вашего бота
     - `ADMIN_ID` - ваш Telegram ID

5. **Запустите деплой:**
   - Нажмите "Create Web Service"
   - Дождитесь завершения деплоя

**Плюсы:** Бесплатный тариф, автоматический деплой

**Примечание:** В проекте уже есть файл `render.yaml` с настройками, Render автоматически его использует.

---

### 3. 🐳 Docker + VPS (DigitalOcean, Hetzner, AWS)

Этот вариант дает больше контроля, но требует больше настроек.

#### Шаги:

1. **Создайте VPS:**
   - DigitalOcean: https://www.digitalocean.com (от $4/месяц)
   - Hetzner: https://www.hetzner.com (от €4/месяц)
   - AWS EC2: https://aws.amazon.com/ec2/

2. **Подключитесь к серверу:**
   ```bash
   ssh root@your_server_ip
   ```

3. **Установите Docker:**
   ```bash
   # Ubuntu/Debian
   curl -fsSL https://get.docker.com -o get-docker.sh
   sh get-docker.sh
   ```

4. **Клонируйте репозиторий:**
   ```bash
   git clone your_repo_url
   cd "Work Bot"
   ```

5. **Создайте .env файл:**
   ```bash
   nano .env
   ```
   Добавьте:
   ```
   BOT_TOKEN=your_telegram_bot_token_here
   ADMIN_ID=your_telegram_user_id_here
   ```

6. **Соберите и запустите Docker контейнер:**
   ```bash
   docker build -t work-bot .
   docker run -d --name work-bot --restart unless-stopped --env-file .env work-bot
   ```

7. **Проверьте логи:**
   ```bash
   docker logs work-bot
   ```

**Плюсы:** Полный контроль, можно использовать для нескольких ботов

---

### 4. 🚁 Fly.io

Fly.io - современная платформа для деплоя приложений.

#### Шаги:

1. **Установите Fly CLI:**
   ```bash
   curl -L https://fly.io/install.sh | sh
   ```

2. **Войдите в Fly.io:**
   ```bash
   fly auth login
   ```

3. **Создайте приложение:**
   ```bash
   fly launch
   ```

4. **Настройте переменные окружения:**
   ```bash
   fly secrets set BOT_TOKEN=your_telegram_bot_token_here
   fly secrets set ADMIN_ID=your_telegram_user_id_here
   ```

5. **Запустите деплой:**
   ```bash
   fly deploy
   ```

**Плюсы:** Бесплатный тариф, хорошая производительность

---

### 6. 📦 Без Docker на VPS (простой вариант)

Если не хотите использовать Docker:

#### Шаги:

1. **Подключитесь к серверу:**
   ```bash
   ssh root@your_server_ip
   ```

2. **Установите Python и зависимости:**
   ```bash
   apt update
   apt install python3 python3-pip git -y
   ```

3. **Клонируйте репозиторий:**
   ```bash
   git clone your_repo_url
   cd "Work Bot"
   ```

4. **Установите зависимости:**
   ```bash
   pip3 install -r requirements.txt
   ```

5. **Создайте .env файл:**
   ```bash
   nano .env
   ```
   Добавьте:
   ```
   BOT_TOKEN=your_telegram_bot_token_here
   ADMIN_ID=your_telegram_user_id_here
   ```

6. **Запустите бота через screen или tmux:**
   ```bash
   # Установите screen
   apt install screen -y
   
   # Создайте новую сессию
   screen -S work-bot
   
   # Запустите бота
   python3 main.py
   
   # Нажмите Ctrl+A, затем D для отсоединения
   ```

7. **Или используйте systemd (для автозапуска):**
   ```bash
   # Создайте файл сервиса
   nano /etc/systemd/system/work-bot.service
   ```
   
   Добавьте:
   ```ini
   [Unit]
   Description=Work Bot Telegram Bot
   After=network.target
   
   [Service]
   Type=simple
   User=root
   WorkingDirectory=/root/Work Bot
   Environment="PATH=/usr/bin:/usr/local/bin"
   ExecStart=/usr/bin/python3 /root/Work Bot/main.py
   Restart=always
   RestartSec=10
   
   [Install]
   WantedBy=multi-user.target
   ```
   
   Запустите:
   ```bash
   systemctl daemon-reload
   systemctl enable work-bot
   systemctl start work-bot
   systemctl status work-bot
   ```

---

## 🔐 Безопасность

1. **Никогда не коммитьте .env файл в Git**
2. **Используйте сильные пароли для сервера**
3. **Настройте firewall (если используете VPS)**
4. **Регулярно обновляйте зависимости**

## 📊 Мониторинг

Для мониторинга работы бота:

- **Railway/Render:** Логи доступны в панели управления
- **VPS:** Используйте `docker logs work-bot` или `journalctl -u work-bot -f`
- **Fly.io:** Используйте `fly logs`

## 🔄 Обновление бота

1. **Railway/Render:** Автоматически обновляется при push в GitHub
2. **Docker:** 
   ```bash
   git pull
   docker build -t work-bot .
   docker stop work-bot
   docker rm work-bot
   docker run -d --name work-bot --restart unless-stopped --env-file .env work-bot
   ```
3. **VPS без Docker:**
   ```bash
   git pull
   systemctl restart work-bot
   ```

## ❓ Частые проблемы

### Бот не отвечает
- Проверьте логи на наличие ошибок
- Убедитесь, что BOT_TOKEN правильный
- Проверьте, что бот запущен

### Ошибки подключения
- Проверьте интернет-соединение сервера
- Убедитесь, что firewall не блокирует исходящие соединения

### Файлы не сохраняются
- Проверьте права доступа к файлам JSON
- Убедитесь, что рабочая директория правильная

## 💡 Рекомендации по бесплатным вариантам

### 🥇 Лучший выбор для начинающих:
**Railway** - самый простой, автоматический деплой, $5 кредитов в месяц (обычно хватает)

### 🥈 Альтернатива:
**Render** - тоже простой, но может засыпать (для ботов это не проблема)

### 🥉 Для более продвинутых:
**Fly.io** - не засыпает, хорошая производительность, 3 бесплатных VM

### 🎯 Для полного контроля (но нужно обновлять задачу):
**PythonAnywhere** - полностью бесплатно, но нужно обновлять задачу раз в день

## 📊 Сравнение бесплатных вариантов

| Платформа | Сложность | Засыпает? | Автообновление | Рекомендация |
|-----------|-----------|-----------|----------------|--------------|
| Railway | ⭐ Легко | ❌ Нет | ✅ Да | 🥇 Лучший выбор |
| Render | ⭐ Легко | ⚠️ Да (15 мин) | ✅ Да | 🥈 Хороший выбор |
| Fly.io | ⭐⭐ Средне | ❌ Нет | ✅ Да | 🥉 Для продвинутых |
| PythonAnywhere | ⭐⭐ Средне | ⚠️ Нужно обновлять | ❌ Нет | Для полного контроля |

## 💰 Платные варианты (если бесплатных не хватает)

- **VPS (DigitalOcean/Hetzner)** - от $4-5/месяц, полный контроль
- **AWS/GCP** - есть бесплатные кредиты для новых пользователей
